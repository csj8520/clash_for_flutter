class ClashVersion {
  bool premium;
  String version;
  ClashVersion({required this.premium, required this.version});
}

class SideBarMenu {
  String label;
  String type;
  SideBarMenu(this.label, this.type);
}
